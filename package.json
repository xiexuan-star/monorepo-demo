{
  "name": "xiex-mono-demo",
  "private": true,
  "packageManager": "^yarn@1.22.0",
  "workspaces": [
    "packages/*",
    "build"
  ],
  "devDependencies": {
    "lerna": "^5.5.0",
    "typescript": "4.6.3"
  },
  "scripts": {
    "dev": "npm run -C packages/docs dev",
    "build:doc": "npm run -C packages/docs build"
  }
}
